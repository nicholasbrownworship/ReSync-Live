# aiortc pulls in PyAV (av) and the crypto/codec libraries it needs.
# No signaling/web framework - the signaling server is hand-written
# against Python's standard library only (see signaling/ws_server.py).
aiortc>=1.9.0

# Used for per-guest audio gain adjustment (sfu/room.py). Almost
# certainly already present as an aiortc transitive dependency, but
# listed explicitly since we import it directly ourselves.
numpy>=1.24.0

# GUI is Tkinter, which ships with Python itself - no entry needed here.

# Packaging the desktop app into a standalone .exe (build-time only,
# not needed to just run `python app.py` from source).
pyinstaller>=6.0.0

# Self-signed TLS cert generation (certs.py) - required for HTTPS/WSS,
# which browsers require before allowing camera/mic access at all.
# Already an aiortc transitive dependency; listed explicitly since we
# import it directly ourselves too.
cryptography>=41.0.0
